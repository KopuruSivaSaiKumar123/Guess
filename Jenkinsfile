pipeline {
    agent any  // This defines the Jenkins agent where the pipeline will run.

    stages {
        stage('Build') {
            steps {
                echo 'Building the project...'
                // Add build commands (e.g., mvn, npm, etc.)
            }
        }
        stage('Test') {
            steps {
                echo 'Running tests...'
                // Add test commands (e.g., unit tests, integration tests)
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying the project...'
                // Add deployment commands (e.g., to server or cloud)
            }
        }
    }
}
